# Parlamento

Small, self-hosted web app.

## Structure

- `frontend/` — browser app
- `backend/` — server code
- `data/` — database schema, migrations, and seed scripts

## Run

```bash
npm install
npm run dev
```

Serves on port `3000`.
