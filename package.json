{
  "name": "parlamento",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "data:import:congress": "node --disable-warning=ExperimentalWarning data/scripts/import-congress-deputies.mjs",
    "dev": "npm run dev --workspace frontend"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
